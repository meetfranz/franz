name: pats-franz
version: "5.0.0"
summary: test
description: test2

grade: stable
confinement: devmode

apps:
  pats-franz:
    command: desktop-launch $SNAP/opt/Franz/franz
    #desktop: usr/share/applications/franz.desktop
    plugs:
     - browser-support
     - network
     - unity7
     - gsettings

parts:
  pats-franz:
    plugin: dump
    source: ./out/franz_5.0.0-beta.15_amd64.deb
    source-type: deb
    after: [desktop-glib-only]
    stage-packages:
      - libappindicator1
      - libx11-dev
      - libxext-dev
      - libxss-dev
      - libxkbfile-dev
      - libgconf2-4
      - libnss3
